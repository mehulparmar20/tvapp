<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- TODO Remove the following "exclude" elements to make them a part of the auto backup -->
    <exclude
        domain="database"
        path="com.oxoo.spagreen.db" />
    <!-- Exclude the shared preferences file that contains the GCM registrationId -->
    <exclude
        domain="sharedpref"
        path="login_status.xml" />
    <exclude
        domain="sharedpref"
        path="com.example.android.tv.recommendations.xml" />
</full-backup-content>
